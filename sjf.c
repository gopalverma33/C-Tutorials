#include<stdio.h>
int main(){
    int n,p[n],bt[n],wt[n];
    float abt = 0,awt =0;
    printf("Enter the number of process");
    scanf("%d",&n);
    printf("Enter the procesess");
    for(int i = 0; i<n; i++){
        scanf("%d",&p[i]);
    printf("%d",p[i]);
    }
    
}